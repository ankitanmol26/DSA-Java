1. What is array?
ANS: Array stores the collection of data of similar data types .

2. Why do arrays exist?
ANS: to store the data of similar data types .

3. Why does indexing start at 0?
ANS: the memory reads the data in addressing format so it reads from 0 and it is very easy to access data . if it didnt start from 0 then the memory have to do one extra bit calcution .so the indexing starts from 0 

4. What is contiguous memory?
ANS: it means the memory is in order and it is fastly is accessable 
ex: 10 20 30 40 

5. Advantages of arrays.
ANS: 1 stores the similar data type 
     2 fast data access using addressing

6. Disadvantages of arrays.
ANS : 1 array size is fixed 

7. Time complexity of:
    Access : O(1)
    Search : O(n)
    Insert : O(n)
    Delete : O(n)
